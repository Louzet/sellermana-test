{% extends 'template.html.twig' %}

{% block htmlTitle %}
    {{ 'game.creation' | trans | capitalize }} {{ parent() }}
{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-md-8 offset-md-2 p-5 form-game-creation">
                <h3 class="text-center">{{ 'game.creation' | trans | title }}</h3>
                {{ form(form) }}
            </div>
        </div>
    </div>
{% endblock %}
